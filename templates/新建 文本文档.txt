<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>View Orders</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(to right, #e1f5fe, #fce4ec);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .card {
            background-color: white;
            padding: 40px 50px;
            border-radius: 18px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
            max-width: 420px;
            width: 100%;
            text-align: center;
        }

        h2 {
            font-size: 26px;
            margin-bottom: 30px;
            color: #333;
        }

        label {
            font-weight: 600;
            display: block;
            text-align: left;
            margin-bottom: 10px;
            color: #555;
        }

        input[type="number"] {
            width: 100%;
            padding: 12px;
            margin-bottom: 25px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        input[type="number"]:focus {
            border-color: #6c63ff;
            box-shadow: 0 0 0 2px rgba(108, 99, 255, 0.2);
            outline: none;
        }

        button {
            background-color: #6c63ff;
            color: white;
            border: none;
            padding: 12px 24px;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #574fd6;
        }
    </style>
</head>
<body>
    <div class="card">
        <h2>📦 View Your Orders</h2>
        <form method="POST" action="{{ url_for('orders') }}">
            <label for="user_id">Enter Your User ID:</label>
            <input type="number" name="user_id" id="user_id" placeholder="e.g. 481" required>
            <button type="submit">View Orders</button>
        </form>
    </div>
</body>
</html>
